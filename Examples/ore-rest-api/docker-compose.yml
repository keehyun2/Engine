version: '3.8'

services:
  ore-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ORE_PATH=/ore/ore
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - ./jobs:/app/jobs
      - ./../Input:/ore-input:ro
      - ./ore-binaries:/ore:ro
    restart: unless-stopped
